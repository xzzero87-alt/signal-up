# v2.2 설정 IA 재설계 — 실행 계획 (Planner 산출물)

> 원본 사양: `시그널 프로그램/handoff/v2.2_settings_ia_redesign.md` (2026-06-08 확정)
> 목업: `시그널 프로그램/mockups/v2.2_settings_redesign_preview.html`
> 본 문서: 다음 세션 구현용 마일스톤 계획. **구현 미착수** (계획만).
> 진행 구조: [ADR-0019](../adr/0019-planner-generator-evaluator-loop.md) Planner→Generator→Evaluator.

## 현황 (코드 조사 완료)

- 재사용: `exchanges/upbit.py:72` `/v1/market/all` 호출 → 코인 API에 활용.
- 변경점: `config.py:176-186` `_parse_whitelist`에 `"화이트리스트는 마켓이 하나 이상 필요합니다"`(line 184) 규칙 존재 → §7-4대로 제거 + model_validator 교체.
- 신규 필요: `web/api/markets.py`, `data/kr_universe`(모듈 또는 JSON).
- 기존 API 디렉터리: `web/api/`에 markets.py 없음 (신규).

## 마일스톤

### M1 — 백엔드 데이터 소스 `feat(api)`
- `GET /api/markets/coins`: 업비트 `/v1/market/all` KRW 마켓 → `[{market, korean_name}]`, 1h 캐시.
  - verify: 200 + KRW 목록 + 캐시 동작.
- `data/kr_universe`(40종, §4) + `GET /api/markets/kr`: `[{code, name, market, sector}]` 섹터 그룹.
  - verify: 섹터·코드 반환, 잔여 코드 교차검증 완료(아래 표).

### M2 — 빈 선택 정책 `fix(config)`
- `_parse_whitelist`(config.py:184) "하나 이상" 규칙 **제거**(빈 코인 허용).
- `model_validator` 추가: `whitelist_markets`·`kr_whitelist_symbols`가 **둘 다 비면 거부**.
- **러너 다운스트림 점검**: 빈 코인 화이트리스트를 가정하는 곳 확인(스캔 루프 등).
- 회귀 테스트: 코인0+국장>0 저장 성공 / 둘 다 0 저장 422.

### M3 — 설정 페이지 재구성 `feat(ui)`
- `settings.html` ①종목설정(코인/국장 탭·검색·체크·선택 칩) + ②전략(기존 카드 아래로 이동).
- 콤마 텍스트 입력 폐지. 제출 시 `whitelist_markets`/`kr_whitelist_symbols` 배열 전송.
- BUG-3 단일 패널 동작 유지, `id`/`name`/`data-field`/`err-` 보존.

### M4 — 시스템 페이지 `feat(ui)`
- `system.html`에 운영·국장연결(KIS)·알림 3섹션. 동일 `PUT /api/settings` 부분 업데이트.

### M5 — 회귀 가드 + 게이트 `test`
- BUG-1(폼 input↔스키마)·BUG-3 가드 + 멀티셀렉트 빈/대량 선택 배열 전송 테스트.
- 게이트 4종: ruff·mypy·pytest(cov≥70)·ui_check.

## 40종 국장 유니버스 (교차검증 상태)

> 검증 출처: 다음금융·KRX·investing 등 (2026-06 조회). 비용 CRITICAL로 대표 대형주 표본 검증 후 중단.
> **남은 코드는 M1에서 잔여 교차검증 필요** (상장폐지·코드변경·시장이전).

| 검증 상태 | 종목 |
|----------|------|
| ✅ 확인됨 | 삼성전자(005930) · SK하이닉스(000660) · LG에너지솔루션(373220) · 알테오젠(196170) · 에코프로비엠(247540) · 에코프로(086520) · HLB(028300) |
| ⚠ 변동 주의 | **알테오젠(196170)**: 코스닥→코스피 이전 준비 중 (2026-06 기준) → `market` 필드 KOSDAQ→KOSPI 변경 가능. M1 구현 시 재확인. |
| ⏳ 잔여 검증 | 나머지 33종 (핸드오프 §4 목록) — 코드·상장상태 미검증. M1에서 일괄 확인. |

핸드오프 §4 전체 40종 목록을 시작점으로 사용하되, M1에서 코드 정확성 1회 교차검증 후 확정.

## 변경 금지 (Generator 가드)

- DESIGN.md §8.1~8.5 도메인 시그니처 불변. 필드 자체 유지, UI 입력 방식만 변경.
- `PUT /api/settings` 계약(부분 업데이트) 유지.
- 자동매매 제외(ADR-0002), 로컬 바인드/마스킹 규칙.

## 미해결 / 다음 세션 결정

- 잔여 33종 코드 교차검증 (비용 CRITICAL로 이번 세션 중단).
- 알테오젠 시장 이전 시 `market` 표기 정책.
- `kr_universe`를 코드 모듈 vs JSON 중 무엇으로 둘지 (구현 시 결정).
